{
  "name": "next-stripe",
  "private": true,
  "version": "0.0.0-semantically-released",
  "scripts": {
    "build": "turbo run build"
  },
  "devDependencies": {
    "prettier": "^2.2.1",
    "turbo": "^1.5.6"
  }
}
